version: 2.1
# orbs:
#   slack: circleci/slack@4.0.0

jobs:
  build:
    docker:
      - image: cimg/python:3.9
    steps:
      - run: pip install coverage
      - run: echo $PATH
version: 2
spin-down-review-env:
  jobs:
      - build
